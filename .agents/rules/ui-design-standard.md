# 🎨 UI Design Standard Rule (MANDATORY)

> **Mục tiêu:** Đảm bảo mọi giao diện được tạo ra phải đạt chuẩn "Premium Engineering" theo phong cách Linear, tuân thủ tuyệt đối các thông số kỹ thuật.

## 🚨 Chỉ thị BẮT BUỘC
Mọi tác vụ liên quan đến **Tạo mới UI, Cấu hình giao diện (CSS/Tailwind), hoặc Thiết kế Components** ĐỀU PHẢI:
1. **Đọc file tri thức thiết kế:** `file:///Users/ttcenter/Documents/Technology/thinkway/DESIGN.md` trước khi bắt đầu.
2. **Tuân thủ Tokens:** Sử dụng chính xác các mã màu (Hex/RGBA), độ bo góc (Radius), và khoảng cách (Spacing) đã định nghĩa.
3. **Typography:** Bắt buộc sử dụng `Inter Variable` với cấu hình font-feature-settings: `"cv01", "ss03"`.
4. **Atmosphere:** Giữ vững tinh thần "Dark-mode-native" với độ trong suốt (Translucency) và phân lớp ánh sáng (Luminance Stacking).

## 🛠️ Quy trình thực thi
- **Bước 1:** Sử dụng `view_file` để đọc `DESIGN.md`.
- **Bước 2:** Đối chiếu yêu cầu của User với các Components mẫu trong `DESIGN.md`.
- **Bước 3:** Triển khai mã nguồn. Tuyệt đối không sử dụng các giá trị màu sắc hoặc font chữ nằm ngoài hệ thống đã quy định.

---
*MILKYWAY-CORE —— Thẩm mỹ là kỷ luật.*
